import 'package:flutter/material.dart';

class AplicacaoForm extends StatelessWidget {
  const AplicacaoForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formulário de Aplicação')),
      body: const Center(child: Text('Formulário de Aplicação')),
    );
  }
}