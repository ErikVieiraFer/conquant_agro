import 'package:flutter/material.dart';

class HistoricoPesagensScreen extends StatelessWidget {
  const HistoricoPesagensScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Histórico de Pesagens')),
      body: const Center(child: Text('Tela de Histórico de Pesagens')),
    );
  }
}