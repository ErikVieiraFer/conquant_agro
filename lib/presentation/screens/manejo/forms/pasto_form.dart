import 'package:flutter/material.dart';

class PastoForm extends StatelessWidget {
  const PastoForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formulário de Pasto')),
      body: const Center(child: Text('Formulário de Pasto')),
    );
  }
}