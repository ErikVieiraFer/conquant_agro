import 'package:flutter/material.dart';

class GadoForm extends StatelessWidget {
  const GadoForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formulário de Gado')),
      body: const Center(child: Text('Formulário de Gado')),
    );
  }
}