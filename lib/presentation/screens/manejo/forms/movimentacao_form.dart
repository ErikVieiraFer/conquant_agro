import 'package:flutter/material.dart';

class MovimentacaoForm extends StatelessWidget {
  const MovimentacaoForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formulário de Movimentação')),
      body: const Center(child: Text('Formulário de Movimentação')),
    );
  }
}