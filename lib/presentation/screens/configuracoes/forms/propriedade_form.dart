import 'package:flutter/material.dart';

class PropriedadeForm extends StatelessWidget {
  const PropriedadeForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Formulário de Propriedade')),
      body: const Center(child: Text('Formulário de Propriedade')),
    );
  }
}